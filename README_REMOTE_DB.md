# 🌐 Подключение к удаленной БД Firebird

## 🎯 Быстрый старт

### 1️⃣ Настройка сервера (10 минут)

**На сервере 85.114.224.45 выполните:**

```powershell
# 1. Открыть PowerShell от администратора

# 2. Проверить Firebird
Get-Service | Where-Object {$_.DisplayName -like "*Firebird*"}

# 3. Открыть порт 3050
New-NetFirewallRule -DisplayName "Firebird Server - Port 3050" -Direction Inbound -LocalPort 3050 -Protocol TCP -Action Allow -Profile Any

# 4. Проверить порт
netstat -an | findstr "3050"
```

**На вашем компьютере проверьте:**

```powershell
Test-NetConnection -ComputerName 85.114.224.45 -Port 3050
```

✅ Должно быть: `TcpTestSucceeded : True`

---

### 2️⃣ Создание конфигурации (2 минуты)

```bash
# Скопировать пример
copy config\remote_db.env.example config\remote_db.env

# Отредактировать config/remote_db.env при необходимости
```

---

### 3️⃣ Тестирование (5 минут)

```bash
# Запустить тесты
python scripts/test_remote_connection.py
```

**Ожидаемый результат:**
```
✅ OK     | Базовое подключение
✅ OK     | Информация о БД
✅ OK     | Простой запрос
✅ OK     | Список магазинов
✅ OK     | Данные о продажах
✅ OK     | Блокировка опасных операций

ПОЗДРАВЛЯЮ! Все тесты пройдены успешно!
```

---

## 💻 Использование в коде

```python
from src.remote_db_connector import RemoteDatabaseConnector

# Создание коннектора
connector = RemoteDatabaseConnector()

# Тест подключения
success, message = connector.test_connection()
print(message)

# Выполнение запроса
df = connector.execute_query_to_dataframe(
    "SELECT FIRST 10 ID, NAME FROM STORGRP"
)
print(df)
```

---

## 🔒 Безопасность

- ✅ **READ-ONLY режим** - изменение данных невозможно
- ✅ **Валидация SQL** - запрещены UPDATE, DELETE, INSERT и т.д.
- ✅ **Таймауты** - автоматическое прерывание долгих операций
- ✅ **Логирование** - все действия записываются
- ✅ **Автозакрытие** - соединения автоматически закрываются

---

## 📚 Документация

- **Полная инструкция**: `docs/SERVER_SETUP_INSTRUCTIONS.md`
- **Быстрые команды**: `docs/SERVER_SETUP_QUICK_COMMANDS.txt`
- **Руководство по безопасности**: `docs/REMOTE_DB_SAFETY_GUIDE.md`
- **Следующие шаги**: `docs/NEXT_STEPS_REMOTE_DB.md`

---

## 🆘 Проблемы?

1. **Проверьте логи**: `logs/coffee_analysis_YYYY-MM-DD.log`
2. **Изучите документацию**: `docs/REMOTE_DB_SAFETY_GUIDE.md`
3. **Запустите тесты**: `python scripts/test_remote_connection.py`

---

## ⚙️ Конфигурация БД

```
Сервер:   85.114.224.45
Порт:     3050
БД:       G:\Гранит\GRANITDB\GEORGIA.GDB
Режим:    READ-ONLY (только чтение)
```

---

## 🚀 Готово к работе!

После успешных тестов можете безопасно работать с удаленной БД.

**Помните: только чтение, никаких изменений! 🔒**

