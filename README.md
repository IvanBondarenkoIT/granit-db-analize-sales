# ☕ Coffee Sales Analysis Tool

GUI приложение для анализа продаж кофе из базы данных Firebird с корректными расчетами сумм, чашек и килограммов.

## 🆕 Новое: Database Proxy API
**Работайте с БД с любого устройства!** 🌍

Создана полная документация для внедрения безопасного REST API шлюза, который позволяет подключаться к Firebird БД с любого IP адреса, обходя ограничения whitelist.

📚 **[Документация по Proxy API →](PROXY_QUICK_START.md)**

**Преимущества:**
- ✅ Доступ с любого устройства и локации
- ✅ Централизованная безопасность
- ✅ READ-ONLY режим
- ✅ Bearer Token аутентификация
- ✅ HTTPS шифрование

**Быстрый старт:** Прочитайте [`PROXY_QUICK_START.md`](PROXY_QUICK_START.md) для начала работы.

### Web Dashboard (Next.js)
- Ветка `web-proxy-ui` содержит веб-дашборд (`web/frontend`) и backend-прокси (`web/backend`).
- README по веб-проекту: `web/README.md`.
- Инструкция по деплою на Railway: `docs/RAILWAY_DEPLOYMENT.md`.

### Flask Monolith (в разработке)
- Ветка `flask-monolith`, директория `webapp/`.
- Flask + Bootstrap (Jinja2), минимум JS, Mobile-first.
- Локальный запуск и деплой: `webapp/README.md` и раздел «Приложение Flask» в `docs/RAILWAY_DEPLOYMENT.md`.

### Использование в GUI

1. Скопируйте `config/proxy_api.env.example` → `config/proxy_api.env` и впишите реальные `PROXY_API_URL` и токены (`PROXY_API_TOKEN`, при необходимости резервный).
2. Убедитесь, что установлены зависимости (`pip install -r requirements.txt`).
3. Для проверки соединения запустите скрипт:
   ```bash
   python scripts/test_proxy_connection.py
   ```
4. В GUI в секции "Подключение к базе данных" выберите режим **"Удаленная БД (через API)"**, при необходимости скорректируйте URL/токены (они подтягиваются из `.env`).
5. После успешного health-check можно работать с отчетами как обычно (режим READ-ONLY).

## 🚀 Быстрый старт

### 1. Установка зависимостей
```bash
# Активация виртуального окружения
python -m venv venv
venv\Scripts\activate

# Установка пакетов
pip install -r requirements.txt
```

### 2. Настройка базы данных
Отредактируйте файл `config.env`:
```env
DB_PATH=D:\Granit DB\GEORGIA.GDB
DB_USER=SYSDBA
DB_PASSWORD=masterkey
DB_CHARSET=UTF8
```

### 3. Запуск приложения
```bash
# GUI приложение
python gui_main.py

# Или с логами
python run_gui_with_logs.py

# Консольное приложение
python app.py
```

## 📁 Структура проекта

```
coffee-sales-analysis/
├── src/                    # Исходный код
│   ├── database_connector.py
│   ├── gui_app.py
│   ├── logger_config.py
│   └── coffee_analysis.py
├── tests/                  # Тестовые скрипты
├── scripts/                # Скрипты запуска
├── docs/                   # Документация
├── data/                   # Данные для сверки
├── logs/                   # Логи приложения
├── output/                 # Результаты экспорта
├── reports/                # Отчеты
├── notebooks/              # Jupyter notebooks
├── venv/                   # Виртуальное окружение
├── config.env              # Настройки БД
├── requirements.txt        # Зависимости Python
├── gui_main.py            # Точка входа GUI
├── app.py                 # Консольное приложение
└── run_gui_with_logs.py   # GUI с логами
```

## 🔧 Основные функции

- **Подключение к БД Firebird** - безопасное подключение с проверкой
- **Анализ продаж кофе** - расчет чашек, килограммов и сумм
- **Гибкие отчеты** - группировка по дням/неделям/месяцам
- **Экспорт в Excel** - сохранение результатов
- **Система логирования** - отладка и мониторинг

## 📊 Логика расчетов

### Суммы продаж
- Общая касса по магазинам (без фильтрации по товарам)
- Источник: `D.SUMMA` из таблицы `STORZAKAZDT`

### Чашки кофе
- Количество чашек по типам (MonoCup, BlendCup, CaotinaCup, AllCup)
- Источник: `GD.Source` из таблицы `STORZDTGDS`

### Килограммы
- Только пачки кофе (не напитки)
- Включает пачки с весом в названии + пачки Caotina
- Логика: количество пачек = килограммы

## 🎯 Статус проекта

✅ **ЗАВЕРШЕН УСПЕШНО!**

- GUI приложение работает корректно
- Все расчеты реализованы правильно
- Система логирования настроена
- Экспорт в Excel работает
- Код готов к продакшену

## 📝 Лицензия

Проект создан для внутреннего использования компании "Дом Кофе".

