# КОНТЕКСТ ПРОЕКТА: Анализ продаж кофе

## 📋 ОПИСАНИЕ ПРОЕКТА

**Название**: Система анализа продаж кофе с GUI интерфейсом  
**База данных**: Firebird 2.5.0 (GEORGIA.GDB)  
**Язык**: Python 3.8+  
**GUI**: Tkinter + ttk  
**Дата создания**: 2025-01-02  

## 🎯 ЦЕЛЬ ПРОЕКТА

Создать GUI приложение для анализа продаж кофе в разрезе магазинов и временных периодов с возможностью:
- Безопасного подключения к Firebird БД
- Генерации отчетов по продажам кофе
- Отображения данных в табличном виде (магазины × периоды)
- Экспорта результатов в Excel

## 🗄️ СТРУКТУРА БАЗЫ ДАННЫХ

### Основные таблицы:
- **STORZAKAZDT** - заказы магазинов
- **STORZDTGDS** - товары в заказах  
- **GOODS** - справочник товаров
- **STORGRP** - группы магазинов
- **GOODSGROUPS** - группы товаров

### Ключевые поля:
- `DAT_` - дата заказа
- `STORGRPID` - ID группы магазина
- `Source` - количество товара
- `OWNER` - ID группы товара (для категоризации кофе)

### Активные магазины (STORGRPID):
- 27, 43, 44, 46

### Типы кофе по OWNER ID:
- **MonoCup**: 24435, 25539, 21671, 25546, 25775, 25777, 25789
- **BlendCup**: 23076, 21882, 25767, 248882, 25788  
- **CaotinaCup**: 24491, 21385

## 📁 СТРУКТУРА ПРОЕКТА

```
Granit DB analize sales/
├── src/                          # Основной код
│   ├── __init__.py
│   ├── database_connector.py     # Подключение к БД
│   ├── coffee_analysis.py        # Анализ данных
│   ├── gui_app.py               # GUI приложение
│   ├── logger_config.py         # Настройка логирования
│   ├── compact_table_view.py    # Компактный вид таблицы
│   └── multi_line_treeview.py   # Многострочный Treeview
├── notebooks/                    # Jupyter notebooks
│   └── coffee_analysis.ipynb
├── output/                       # Результаты анализа
├── logs/                         # Логи приложения
├── main.py                       # Консольная версия
├── gui_main.py                   # Точка входа GUI
├── run_gui_with_logs.py         # GUI с логированием
├── requirements.txt              # Зависимости
├── config.env                    # Настройки БД
├── DATABASE_ATLAS.md            # Описание БД
├── GUI_INSTRUCTIONS.md          # Инструкции по GUI
└── PROJECT_SUMMARY.md           # Резюме проекта
```

## 🔧 ТЕХНИЧЕСКИЕ ДЕТАЛИ

### Зависимости:
- `fdb` - подключение к Firebird
- `pandas` - обработка данных
- `matplotlib`, `seaborn`, `plotly` - визуализация
- `tkinter` - GUI
- `python-dotenv` - переменные окружения
- `openpyxl` - экспорт в Excel

### Настройки БД (config.env):
```
DB_PATH=D:\Granit DB\GEORGIA.GDB
DB_USER=SYSDBA
DB_PASSWORD=masterkey
DB_CHARSET=UTF8
```

## 🎨 ОСОБЕННОСТИ GUI

### Интерфейс состоит из 5 секций:
1. **Подключение к БД** - настройки подключения
2. **Параметры отчета** - период, группировка, магазины
3. **Генерация отчета** - кнопки действий
4. **Стиль отображения** - подробный/компактный
5. **Результаты** - таблица с данными

### Формат отчета:
- **Строки**: магазины
- **Колонки**: временные периоды (дни/недели/месяцы)
- **Ячейки**: 3 строки данных:
  - Чашки (шт)
  - Килограммы (кг) 
  - Общая сумма продаж

### Стили отображения:
- **Подробный**: "Чашки: 25 шт\nКг: 6.25 кг\nСумма: 1250.50"
- **Компактный**: "☕ 25шт\n📦 6.3кг\n💰 1251"

## 📊 SQL ЗАПРОСЫ

### Основной запрос для получения данных:
```sql
SELECT 
    stgp.name,
    SUM(CASE WHEN G.OWNER IN ('24435','25539','21671','24435','25546','25775','25777','25789') 
        THEN GD.Source ELSE NULL END) AS MonoCup,
    SUM(CASE WHEN G.OWNER IN ('23076','21882','25767','248882','25788') 
        THEN GD.Source ELSE NULL END) AS BlendCup,
    SUM(CASE WHEN G.OWNER IN ('24491','21385') 
        THEN GD.Source ELSE NULL END) AS CaotinaCup,
    SUM(CASE WHEN G.OWNER IN ('24435','25539','21671','24435','25546','25775','25777','25789','23076','21882','25767','248882','25788','24491','21385') 
        THEN GD.Source ELSE NULL END) AS AllCup,
    D.DAT_
FROM storzakazdt D 
JOIN STORZDTGDS GD ON D.ID = GD.SZID 
JOIN Goods G ON GD.GodsID = G.ID 
JOIN storgrp stgp ON D.storgrpid = stgp.id 
LEFT JOIN goodsgroups GG ON G.owner = GG.id 
WHERE D.STORGRPID IN ('27','43','44','46') 
    AND D.CSDTKTHBID IN ('1', '2', '3','5') 
    AND D.DAT_ >= ? AND D.DAT_ <= ?
    AND NOT (D.comment LIKE '%мы;%' OR D.comment LIKE '%Мы;%' OR D.comment LIKE '%Тестирование%')
GROUP BY stgp.name, D.Dat_
```

## 🚀 КОМАНДЫ ДЛЯ ЗАПУСКА

### Активация окружения:
```bash
venv\Scripts\activate
```

### Запуск GUI:
```bash
python run_gui_with_logs.py
```

### Консольная версия:
```bash
python main.py
```

## 📝 ЛОГИРОВАНИЕ

- **Файлы логов**: `logs/coffee_analysis_YYYYMMDD.log`
- **Ротация**: ежедневная
- **Уровни**: INFO, WARNING, ERROR
- **Формат**: `%(asctime)s - %(name)s - %(levelname)s - %(message)s`

## 🔍 ОСОБЕННОСТИ РЕАЛИЗАЦИИ

### Обработка данных:
- Извлечение веса из названий товаров (regex)
- Расчет общего веса: `QUANTITY * 0.25` кг
- Группировка по типам кофе
- Pivot таблица для отображения

### GUI компоненты:
- `ttk.Treeview` с многострочными ячейками
- Высота строк: 70px
- Ширина колонок: 160px (периоды), 180px (магазины)
- Стили для разных режимов отображения

### Обработка ошибок:
- Проверка подключения к БД
- Валидация параметров
- Логирование всех операций
- Graceful handling исключений

## 📈 РЕЗУЛЬТАТЫ

### Созданные файлы:
- GUI приложение с полным функционалом
- Система логирования
- Экспорт в Excel
- Документация проекта
- Jupyter notebook для анализа

### Ключевые достижения:
- ✅ Безопасное подключение к Firebird БД
- ✅ Интуитивный GUI интерфейс
- ✅ Гибкая настройка отчетов
- ✅ Многострочное отображение данных
- ✅ Экспорт результатов
- ✅ Полное логирование операций

## 🔮 ВОЗМОЖНЫЕ УЛУЧШЕНИЯ

1. **Дополнительные фильтры** - по типам кофе, магазинам
2. **Графики и диаграммы** - визуализация трендов
3. **Сравнение периодов** - год к году, месяц к месяцу
4. **Настройки пользователя** - сохранение предпочтений
5. **Многопоточность** - для больших объемов данных
6. **Кэширование** - для ускорения повторных запросов

## 📞 КОНТАКТЫ И ПОДДЕРЖКА

- **Проект**: Анализ продаж кофе
- **База данных**: D:\Granit DB\GEORGIA.GDB
- **Пользователь БД**: SYSDBA
- **Дата последнего обновления**: 2025-01-02

---

*Этот файл содержит полный контекст проекта для быстрого восстановления понимания в будущих сессиях разработки.*
