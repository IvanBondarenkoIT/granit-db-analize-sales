# 🔧 Инструкция по настройке сервера для удаленного подключения к Firebird

## 📋 КРАТКАЯ ИНФОРМАЦИЯ

- **Сервер**: 85.114.224.45:7707
- **ОС**: Windows Server (предположительно)
- **БД**: Firebird 2.5.0
- **Путь к БД**: D:\Granit DB\GEORGIA.GDB
- **Порт Firebird**: 3050 (стандартный)

---

## ⚠️ ВАЖНО ПЕРЕД НАЧАЛОМ

1. **Резервная копия**: Сделайте backup БД перед любыми изменениями
2. **Права администратора**: Все команды требуют прав администратора
3. **Доступ к серверу**: Нужен RDP доступ к серверу

---

## 🚀 ПОШАГОВАЯ ИНСТРУКЦИЯ

### ШАГ 1: ПОДКЛЮЧЕНИЕ К СЕРВЕРУ

1. **Откройте "Подключение к удаленному рабочему столу" (mstsc.exe)**
   - Нажмите `Win + R`
   - Введите `mstsc`
   - Нажмите Enter

2. **Введите данные подключения:**
   ```
   Компьютер: 85.114.224.45:7707
   Пользователь: USER-PC\Ivan
   ```

3. **Подключитесь к серверу**

---

### ШАГ 2: ПРОВЕРКА FIREBIRD SERVER

#### 2.1 Проверка, что Firebird установлен и запущен

**Вариант A: Через Диспетчер задач**
1. Нажмите `Ctrl + Shift + Esc`
2. Перейдите на вкладку "Службы"
3. Найдите службу с именем `FirebirdServerDefaultInstance` или `Firebird Server`
4. Убедитесь, что статус "Работает"

**Вариант B: Через PowerShell (рекомендуется)**
1. Откройте PowerShell **от имени администратора**
   - Нажмите `Win + X`
   - Выберите "Windows PowerShell (администратор)"

2. Выполните команду:
   ```powershell
   Get-Service | Where-Object {$_.DisplayName -like "*Firebird*"}
   ```

**Ожидаемый результат:**
```
Status   Name               DisplayName
------   ----               -----------
Running  FirebirdServerD... Firebird Server - DefaultInstance
```

#### 2.2 Если Firebird НЕ запущен

```powershell
# Запустить службу Firebird
Start-Service -Name "FirebirdServerDefaultInstance"

# Настроить автозапуск
Set-Service -Name "FirebirdServerDefaultInstance" -StartupType Automatic
```

---

### ШАГ 3: НАЙТИ ПУТЬ К FIREBIRD

```powershell
# Найти, где установлен Firebird
Get-Service | Where-Object {$_.DisplayName -like "*Firebird*"} | ForEach-Object {
    $service = Get-WmiObject -Class Win32_Service -Filter "Name='$($_.Name)'"
    $service.PathName
}
```

**Типичные пути установки:**
- `C:\Program Files\Firebird\Firebird_2_5\`
- `C:\Program Files (x86)\Firebird\Firebird_2_5\`
- `D:\Firebird\`

**Запишите путь!** Например: `C:\Program Files\Firebird\Firebird_2_5\`

---

### ШАГ 4: ПРОВЕРКА КОНФИГУРАЦИИ FIREBIRD

#### 4.1 Открыть файл firebird.conf

1. Перейдите в папку установки Firebird (из Шага 3)
2. Откройте файл `firebird.conf` в Блокноте **от имени администратора**
   ```
   C:\Program Files\Firebird\Firebird_2_5\firebird.conf
   ```

#### 4.2 Найти и проверить следующие параметры

**A) RemoteServicePort** (порт для удаленных подключений)
```ini
# Найдите строку:
#RemoteServicePort = 3050

# Если есть символ # в начале, удалите его:
RemoteServicePort = 3050
```

**B) RemoteBindAddress** (адреса для прослушивания)
```ini
# Найдите строку:
#RemoteBindAddress = 

# Оставьте закомментированной (или пустой) для прослушивания на всех адресах
# ИЛИ укажите конкретный IP сервера
```

**C) WireCrypt** (шифрование соединения - для Firebird 3.0+, в 2.5 может не быть)
```ini
# Если найдете:
#WireCrypt = Enabled

# Оставьте как есть или установите:
WireCrypt = Enabled
```

**D) RemoteAccess** (разрешение удаленного доступа)
```ini
# Убедитесь, что НЕТ строки:
# RemoteAccess = false

# Или она закомментирована:
#RemoteAccess = false
```

#### 4.3 Сохранить изменения

1. Сохраните файл `firebird.conf`
2. **Перезапустите службу Firebird:**

```powershell
Restart-Service -Name "FirebirdServerDefaultInstance"
```

---

### ШАГ 5: ОТКРЫТИЕ ПОРТА В WINDOWS FIREWALL ⚡ КРИТИЧЕСКИ ВАЖНО

#### 5.1 Проверить, открыт ли порт

```powershell
# Проверить существующие правила
Get-NetFirewallRule | Where-Object {$_.DisplayName -like "*Firebird*" -or $_.DisplayName -like "*3050*"}
```

#### 5.2 Создать правило для входящих подключений

```powershell
# Создать правило для порта 3050
New-NetFirewallRule -DisplayName "Firebird Server - Port 3050" `
    -Direction Inbound `
    -LocalPort 3050 `
    -Protocol TCP `
    -Action Allow `
    -Profile Any `
    -Description "Allow remote connections to Firebird Database Server"
```

**Ожидаемый результат:**
```
DisplayName                    : Firebird Server - Port 3050
Direction                      : Inbound
Action                         : Allow
Enabled                        : True
```

#### 5.3 Проверить, что правило создано

```powershell
Get-NetFirewallRule -DisplayName "Firebird Server - Port 3050" | Select-Object DisplayName, Enabled, Direction, Action
```

#### 5.4 Альтернативный способ через GUI

1. Откройте `Панель управления` → `Брандмауэр Windows` → `Дополнительные параметры`
2. Нажмите "Правила для входящих подключений"
3. Нажмите "Создать правило..."
4. Выберите "Для порта" → Далее
5. Выберите TCP, укажите порт `3050` → Далее
6. Выберите "Разрешить подключение" → Далее
7. Выберите все профили (Доменный, Частный, Публичный) → Далее
8. Введите имя: "Firebird Server - Port 3050" → Готово

---

### ШАГ 6: ПРОВЕРКА ДОСТУПНОСТИ ПОРТА

#### 6.1 Проверка с самого сервера (локально)

```powershell
# Проверить, слушает ли Firebird порт 3050
netstat -an | findstr "3050"
```

**Ожидаемый результат:**
```
TCP    0.0.0.0:3050           0.0.0.0:0              LISTENING
TCP    [::]:3050              [::]:0                 LISTENING
```

#### 6.2 Узнать IP адрес сервера

```powershell
# Узнать все IP адреса сервера
Get-NetIPAddress -AddressFamily IPv4 | Where-Object {$_.IPAddress -ne "127.0.0.1"} | Select-Object IPAddress, InterfaceAlias
```

**Запишите внутренний IP адрес!** (например, 192.168.1.100)

---

### ШАГ 7: ПРОВЕРКА ВНЕШНЕГО ДОСТУПА К ПОРТУ (С ВАШЕГО КОМПЬЮТЕРА)

После выполнения всех шагов на сервере, с вашего компьютера:

#### 7.1 Проверка через PowerShell

```powershell
# Проверить доступность порта 3050 на сервере
Test-NetConnection -ComputerName 85.114.224.45 -Port 3050
```

**Ожидаемый результат (успех):**
```
ComputerName     : 85.114.224.45
RemoteAddress    : 85.114.224.45
RemotePort       : 3050
TcpTestSucceeded : True
```

**Если TcpTestSucceeded: False** - порт заблокирован (firewall, роутер)

#### 7.2 Проверка через Telnet (альтернатива)

```cmd
telnet 85.114.224.45 3050
```

Если подключение успешно - увидите черный экран или символы (это нормально).

---

### ШАГ 8: НАСТРОЙКА РОУТЕРА (ЕСЛИ НУЖНО) 🌐

Если сервер находится за роутером/NAT:

1. **Войдите в настройки роутера**
2. **Найдите раздел "Port Forwarding" (Проброс портов)**
3. **Создайте правило:**
   - External Port: `3050`
   - Internal IP: `<внутренний IP сервера из Шага 6.2>`
   - Internal Port: `3050`
   - Protocol: `TCP`

4. **Сохраните настройки**

---

### ШАГ 9: ПРОВЕРКА ПОДКЛЮЧЕНИЯ К БД

#### 9.1 Через isql (на сервере)

```cmd
# Перейдите в папку Firebird\bin
cd "C:\Program Files\Firebird\Firebird_2_5\bin"

# Подключитесь к БД локально
isql -user SYSDBA -password masterkey "D:\Granit DB\GEORGIA.GDB"
```

**Команды для проверки:**
```sql
-- Показать версию БД
SHOW VERSION;

-- Показать таблицы
SHOW TABLES;

-- Выйти
QUIT;
```

#### 9.2 Через isql удаленно (с вашего компьютера, если установлен Firebird)

```cmd
isql -user SYSDBA -password masterkey "85.114.224.45:D:\Granit DB\GEORGIA.GDB"
```

---

### ШАГ 10: БЕЗОПАСНОСТЬ 🔒

#### 10.1 ОБЯЗАТЕЛЬНО: Сменить пароль SYSDBA

```cmd
# На сервере в папке Firebird\bin
gsec -user SYSDBA -password masterkey

# В консоли gsec:
modify SYSDBA -pw НОВЫЙ_СЛОЖНЫЙ_ПАРОЛЬ
quit
```

**Запишите новый пароль в безопасное место!**

#### 10.2 Создать отдельного пользователя для приложения

```cmd
# В gsec:
add ANALYTICS_USER -pw СЛОЖНЫЙ_ПАРОЛЬ
quit
```

#### 10.3 Ограничить доступ по IP (опционально)

В `firebird.conf` можно настроить ограничения, но это требует дополнительной настройки.

**Рекомендация:** Использовать firewall правило с ограничением по IP:

```powershell
# Удалить старое правило
Remove-NetFirewallRule -DisplayName "Firebird Server - Port 3050"

# Создать новое с ограничением по IP вашего компьютера
New-NetFirewallRule -DisplayName "Firebird Server - Port 3050 (Restricted)" `
    -Direction Inbound `
    -LocalPort 3050 `
    -Protocol TCP `
    -Action Allow `
    -RemoteAddress "ВАШ_IP_АДРЕС" `
    -Description "Allow Firebird connections only from specific IP"
```

---

## ✅ ЧЕКЛИСТ ПРОВЕРКИ

После выполнения всех шагов, проверьте:

- [ ] Служба Firebird запущена и работает
- [ ] Файл firebird.conf настроен (порт 3050, удаленный доступ разрешен)
- [ ] Служба Firebird перезапущена после изменений
- [ ] Правило Windows Firewall создано для порта 3050
- [ ] Порт 3050 слушается на 0.0.0.0 (netstat)
- [ ] Порт 3050 доступен снаружи (Test-NetConnection)
- [ ] Проброс портов настроен на роутере (если нужно)
- [ ] Подключение к БД работает локально (isql)
- [ ] Пароль SYSDBA изменен на более сложный
- [ ] Записаны новые учетные данные

---

## 🐛 ВОЗМОЖНЫЕ ПРОБЛЕМЫ И РЕШЕНИЯ

### Проблема 1: Служба Firebird не запускается

**Решение:**
```powershell
# Проверить логи
Get-EventLog -LogName Application -Source "Firebird*" -Newest 10
```

### Проблема 2: Порт 3050 не слушается

**Решение:**
- Проверить firebird.conf (RemoteServicePort)
- Перезапустить службу
- Проверить, не занят ли порт другой программой

### Проблема 3: TcpTestSucceeded: False

**Возможные причины:**
1. Windows Firewall блокирует
2. Антивирус блокирует
3. Роутер не пробрасывает порт
4. ISP блокирует порт

**Решение:**
- Проверить все firewall правила
- Временно отключить антивирус для теста
- Проверить настройки роутера
- Связаться с ISP

### Проблема 4: Connection rejected by remote interface

**Решение:**
- Проверить RemoteAccess в firebird.conf
- Проверить, что БД файл существует по указанному пути
- Проверить права доступа к файлу БД

---

## 📞 СЛЕДУЮЩИЙ ШАГ

После успешной настройки сервера:

1. **Сообщите мне:**
   - ✅ Все шаги выполнены
   - ✅ Порт доступен (результат Test-NetConnection)
   - ✅ Новые учетные данные (если изменили)

2. **Я создам:**
   - Модуль `remote_db_connector.py`
   - Тестовый скрипт для проверки подключения
   - Обновлю GUI для поддержки удаленной БД

---

## 📝 ЗАМЕТКИ

- **Не забудьте сделать backup БД!**
- **Запишите все пароли в безопасное место**
- **Протестируйте подключение перед использованием в production**



