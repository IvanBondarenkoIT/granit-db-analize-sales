# 🔒 Руководство по безопасной работе с удаленной БД

## ⚠️ КРИТИЧЕСКИ ВАЖНО

**ЭТО РАБОЧАЯ БАЗА ДАННЫХ!** Любые ошибки могут нарушить работу бизнеса.

### Основные правила:

1. **ТОЛЬКО ЧТЕНИЕ** - Никаких изменений данных!
2. **ТЕСТИРОВАНИЕ** - Сначала тестируем локально
3. **ЛОГИРОВАНИЕ** - Все действия записываются в логи
4. **ОСТОРОЖНОСТЬ** - Дважды проверяем запросы

---

## 🛡️ Механизмы безопасности

### 1. READ-ONLY режим

Модуль `RemoteDatabaseConnector` автоматически работает в режиме только чтения:

```python
# READ-ONLY режим включен ВСЕГДА
connector = RemoteDatabaseConnector(read_only=True)  # По умолчанию True

# Попытка отключить READ-ONLY игнорируется
connector = RemoteDatabaseConnector(read_only=False)  # Все равно будет True!
```

### 2. Валидация SQL запросов

Все запросы проверяются **ПЕРЕД** выполнением:

#### ✅ Разрешенные операции:
- `SELECT` - чтение данных
- `WITH` - CTE (Common Table Expressions)

#### 🚫 Запрещенные операции:
- `INSERT` - вставка данных
- `UPDATE` - изменение данных
- `DELETE` - удаление данных
- `DROP` - удаление объектов
- `ALTER` - изменение структуры
- `TRUNCATE` - очистка таблиц
- `CREATE` - создание объектов
- `GRANT/REVOKE` - управление правами
- `EXECUTE BLOCK` - выполнение блоков кода
- `EXECUTE PROCEDURE` - вызов процедур
- Множественные запросы (`;`)

### 3. Таймауты

Автоматическое прерывание долгих операций:

```python
connector = RemoteDatabaseConnector(
    connection_timeout=10,  # Таймаут подключения: 10 сек
    query_timeout=30        # Таймаут запроса: 30 сек
)
```

### 4. Логирование

Все операции записываются в лог-файл:
- Время подключения/отключения
- Все выполняемые запросы
- Ошибки и предупреждения
- Заблокированные опасные операции

**Файл лога:** `logs/coffee_analysis_YYYY-MM-DD.log`

### 5. Автоматическое закрытие соединений

Использование контекстного менеджера гарантирует закрытие соединения:

```python
with connector.get_connection() as conn:
    # Работа с БД
    pass
# Соединение автоматически закрыто
```

---

## 📋 Примеры использования

### Базовое подключение и тест

```python
from src.remote_db_connector import RemoteDatabaseConnector
from src.logger_config import setup_logger

logger = setup_logger()

# Создание коннектора
connector = RemoteDatabaseConnector()

# Тест подключения
success, message = connector.test_connection()
print(message)
```

### Получение информации о БД

```python
# Безопасное получение информации
db_info = connector.get_database_info()

print(f"Версия Firebird: {db_info['firebird_version']}")
print(f"Количество таблиц: {db_info['tables_count']}")
print(f"READ-ONLY режим: {db_info['read_only']}")
```

### Выполнение SELECT запроса

```python
# Способ 1: Получение списка строк
query = "SELECT ID, NAME FROM STORGRP WHERE ID < ?"
results = connector.execute_query(query, params=(10,))

for row in results:
    print(f"ID: {row[0]}, Name: {row[1]}")
```

```python
# Способ 2: Получение DataFrame (удобнее)
import pandas as pd

query = "SELECT ID, NAME FROM STORGRP WHERE ID < ?"
df = connector.execute_query_to_dataframe(query, params=(10,))

print(df)
```

### Безопасная работа с большими данными

```python
# Используем FIRST для ограничения результатов
query = """
SELECT FIRST 1000
    D.DAT_,
    stgp.NAME,
    D.ALLCUP
FROM STORZAKAZDT D
JOIN STORGRP stgp ON D.STORGRPID = stgp.ID
WHERE D.DAT_ >= ?
ORDER BY D.DAT_ DESC
"""

df = connector.execute_query_to_dataframe(
    query, 
    params=('2025-09-01',)
)

print(f"Получено записей: {len(df)}")
```

---

## 🚫 Примеры заблокированных операций

### Попытка UPDATE

```python
# ЭТО БУДЕТ ЗАБЛОКИРОВАНО!
query = "UPDATE STORGRP SET NAME = 'Test'"

try:
    connector.execute_query(query)
except ValueError as e:
    print(e)  # "🚫 ОПАСНЫЙ ЗАПРОС ЗАБЛОКИРОВАН!"
```

### Попытка DELETE

```python
# ЭТО БУДЕТ ЗАБЛОКИРОВАНО!
query = "DELETE FROM STORGRP WHERE ID = 1"

try:
    connector.execute_query(query)
except ValueError as e:
    print(e)  # "🚫 ОПАСНЫЙ ЗАПРОС ЗАБЛОКИРОВАН!"
```

### Попытка INSERT

```python
# ЭТО БУДЕТ ЗАБЛОКИРОВАНО!
query = "INSERT INTO STORGRP (NAME) VALUES ('Test')"

try:
    connector.execute_query(query)
except ValueError as e:
    print(e)  # "🚫 ОПАСНЫЙ ЗАПРОС ЗАБЛОКИРОВАН!"
```

---

## ✅ Чеклист перед работой с удаленной БД

Перед каждым сеансом работы:

- [ ] Убедитесь, что используете `RemoteDatabaseConnector`
- [ ] Проверьте, что READ-ONLY режим активен
- [ ] Запустите тест подключения (`test_connection()`)
- [ ] Проверьте логи на наличие ошибок
- [ ] Используйте `FIRST N` для ограничения результатов
- [ ] Тестируйте сложные запросы на локальной БД
- [ ] Используйте параметризованные запросы (защита от SQL-инъекций)

---

## 🧪 Тестирование перед работой

**ОБЯЗАТЕЛЬНО запустите тесты перед первым использованием:**

```bash
# Запуск полного набора тестов
python scripts/test_remote_connection.py
```

Тесты проверяют:
1. ✅ Подключение к БД
2. ✅ Получение информации
3. ✅ Выполнение SELECT запросов
4. ✅ Блокировку опасных операций
5. ✅ Работу таймаутов
6. ✅ Логирование

**Все тесты должны быть пройдены!**

---

## 🔧 Конфигурация

### Файл конфигурации

Создайте файл `config/remote_db.env`:

```env
REMOTE_DB_HOST=85.114.224.45
REMOTE_DB_PORT=3050
REMOTE_DB_PATH=G:\Гранит\GRANITDB\GEORGIA.GDB
REMOTE_DB_USER=SYSDBA
REMOTE_DB_PASSWORD=ваш_пароль

REMOTE_CONNECTION_TIMEOUT=10
REMOTE_QUERY_TIMEOUT=30
REMOTE_READ_ONLY=True
REMOTE_MAX_RETRIES=3
REMOTE_LOG_QUERIES=True
```

### Загрузка конфигурации

```python
from dotenv import load_dotenv
import os

# Загрузить конфигурацию
load_dotenv('config/remote_db.env')

# Создать коннектор (параметры из .env)
connector = RemoteDatabaseConnector()
```

---

## ⚠️ Что делать при ошибках

### Ошибка подключения

```
❌ Ошибка подключения к удаленной БД
```

**Проверьте:**
1. Доступность сервера: `ping 85.114.224.45`
2. Открыт ли порт 3050 на сервере
3. Правильность пути к БД: `G:\Гранит\GRANITDB\GEORGIA.GDB`
4. Учетные данные (логин/пароль)

### Ошибка "Permission denied"

```
❌ Permission denied for operation
```

**Решение:**
- Убедитесь, что используете READ-ONLY режим
- Проверьте права пользователя в БД
- Попробуйте переподключиться

### Таймаут запроса

```
❌ Query timeout exceeded
```

**Решение:**
- Оптимизируйте запрос (добавьте индексы)
- Используйте `FIRST N` для ограничения
- Увеличьте `query_timeout` (осторожно!)
- Разбейте запрос на несколько частей

---

## 📞 Поддержка

При возникновении проблем:

1. **Проверьте логи:** `logs/coffee_analysis_YYYY-MM-DD.log`
2. **Запустите тесты:** `python scripts/test_remote_connection.py`
3. **Проверьте конфигурацию:** `config/remote_db.env`
4. **Свяжитесь с администратором БД**

---

## 📝 Лучшие практики

### ✅ Хорошо

```python
# Ограничение результатов
query = "SELECT FIRST 100 * FROM STORGRP"

# Параметризованные запросы
query = "SELECT * FROM STORGRP WHERE ID = ?"
connector.execute_query(query, params=(5,))

# Использование контекстного менеджера
with connector.get_connection() as conn:
    # работа
    pass

# Обработка ошибок
try:
    df = connector.execute_query_to_dataframe(query)
except Exception as e:
    logger.error(f"Ошибка: {e}")
```

### ❌ Плохо

```python
# Без ограничения (может вернуть миллионы строк)
query = "SELECT * FROM STORZAKAZDT"

# Конкатенация строк (SQL-инъекция!)
store_id = "5"
query = f"SELECT * FROM STORGRP WHERE ID = {store_id}"

# Без обработки ошибок
df = connector.execute_query_to_dataframe(query)  # Может упасть

# Попытка изменить данные
query = "UPDATE STORGRP SET NAME = 'Test'"  # ЗАБЛОКИРОВАНО!
```

---

## 🎯 Резюме

**Помните:**

1. 🔒 **Безопасность превыше всего** - это рабочая БД
2. 📖 **Только чтение** - никаких изменений
3. 🧪 **Тестируйте** - перед работой запустите тесты
4. 📝 **Логируйте** - все действия записываются
5. ⚡ **Оптимизируйте** - используйте FIRST, WHERE, индексы
6. 🛡️ **Параметризуйте** - защита от SQL-инъекций
7. 🔄 **Обрабатывайте ошибки** - всегда используйте try/except

**УДАЧНОЙ И БЕЗОПАСНОЙ РАБОТЫ!** 🚀

