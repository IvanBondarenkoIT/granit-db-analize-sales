# 🌐 Web UI для Firebird Proxy API — План разработки

**Дата:** 7 ноября 2025  
**Ветка:** `web-proxy-ui`  
**Цель:** Создать современный, адаптивный (mobile-first) веб-интерфейс, работающий исключительно через существующий REST Proxy API (без прямого подключения к БД). Деплой на Railway.

---

## 1. Контекст и требования

### 1.1. Текущее состояние
- Есть рабочий Proxy API (FastAPI), который отдаёт данные Firebird только через `SELECT`.
- Desktop GUI умеет работать через этот API.
- Требуется веб-версия с тем же функционалом (просмотр продаж кофе, фильтры, отчёты).

### 1.2. Ограничения
- **Без прямого SQL:** клиентское приложение общается только с REST API.
- **Без хранения секретов в браузере:** токены должны выдаваться через backend (или защищённое хранилище на сервере), но не вшиты в JS.
- **Mobile-first:** интерфейс должен корректно работать на смартфонах (адаптивная вёрстка, крупные элементы UI).
- **Railway деплой:** backend и frontend (если SSR) хостятся на Railway, как отдельный сервис/проекта.
- **Modern stack & best practices:** чистая архитектура, разделение клиент/сервер, ESLint/Prettier, типизация.

### 1.3. Ключевые сценарии
- Авторизованный пользователь (через собственный токен/логин) получает доступ к дашборду.
- Просмотр магазинов, фильтрация по датам, группировка по периодам (день/неделя/месяц).
- Таблицы с сортировкой и фильтрами на клиенте (JS).
- Графики (опционально) — рассмотреть Chart.js / ECharts.
- Экспорт в CSV/Excel (можно реализовать на клиенте или через backend).

---

## 2. Архитектура

### 2.1. Общая схема

```
Browser (React/Vue/Svelte) ↔ Web Backend (FastAPI/Next.js API routes) ↔ Proxy API ↔ Firebird
```

### 2.2. Компоненты
- **Frontend (SPA или SSR):**
  - Framework: `Next.js 14` (React + App Router) или `Nuxt 3` (Vue).
  - UI библиотека: `Tailwind CSS` + `Headless UI` (для быстроты и mobile-first подхода).
  - State/Data fetching: `React Query (TanStack Query)` или встроенный `fetch` с SWR.
  - Табличный компонент: `TanStack Table` или `AG Grid` (сортировка/фильтры).
  - PWA/Responsive: медиа-запросы, гибкая сетка (CSS Grid/Flex).

- **Backend (посредник):**
  - Лёгкий FastAPI сервис (или Next.js API routes) для хранения токенов и проксирования запросов к Proxy API.
  - Авторизация пользователей (basic auth/JWT) + Rate limiting.
  - Кеширование ответов (Redis или in-memory cache по необходимости).

- **Deployment:**
  - Railway — два сервиса (frontend + backend) или монолитный Next.js.
  - CI/CD: GitHub Actions (линтер + тесты + авто-deploy).

---

## 3. Технологический стек (рекомендация)

| Слой | Технологии |
|------|-------------|
| Frontend | Next.js 14 (App Router), TypeScript, Tailwind CSS, TanStack Query/Table, ESLint, Prettier |
| Backend | FastAPI (Python 3.11+), httpx/requests для Proxy API, Pydantic, Uvicorn |
| Auth | Simple JWT (FastAPI Users) или Basic auth (если достаточно) |
| Deployment | Railway (Docker), GitHub Actions |
| Monitoring | Railway Logs + Sentry (опционально) |

Альтернатива: monorepo с Remix/Next.js + server actions (тогда Python backend не нужен, но нужен механизм хранения токена на сервере Node.js). Рассмотреть после оценки.

---

## 4. Этапы разработки

### Этап 0. Инициализация (1 день)
1. Создать структуру проекта `web/` внутри репо или отдельный репозиторий (обсудить).
2. Настроить базовый Next.js (или FastAPI + React) шаблон, линтеры, форматтеры, husky.
3. Создать `.env.example` для веб-сервиса (Proxy API URL, токены через backend).

### Этап 1. Backend-прокси (1-2 дня)
1. FastAPI приложение с REST эндпоинтами:
   - `/auth/login` — получение пользовательского токена (из собственной БД/конфига).
   - `/sales` — проксирование запросов (параметры: магазины, даты, группировка).
   - `/stores` — список магазинов.
2. Хранение Proxy API токена на сервере (напр. в переменных окружения).
3. Логирование, обработка ошибок, rate limiting.
4. Юнит-тесты (pytest + httpx).

### Этап 2. Frontend (3-4 дня)
1. Layout + навигация (mobile-first).
2. Страница авторизации (форма логина в backend).
3. Дашборд:
   - Отбор периодов, магазинов (multiselect).
   - Таблица с данными (fetch из backend, клиентские фильтры).
   - Карточки/графики (этап 2.5).
4. Состояние: React Query + optimistic updates.
5. Тесты: Jest + Playwright (smoke tests).

### Этап 3. Deployment & UX (1-2 дня)
1. Dockerfile + Railway configuration (`railway.json`).
2. CI/CD — авто-deploy с ветки `web-proxy-ui`.
3. Monitoring & alerting (Sentry/Friendly Captor).
4. Финальный UX-полиш (Skeleton loaders, empty states, ошибки).

### Этап 4. Документация и handover (0.5 дня)
1. README для веб-приложения.
2. Инструкции по запуску, выдаче доступов, обновлению токенов.
3. Скриншоты/видео (опционально).

---

## 5. План задач (ToDo)

1. ⚙️ **Setup**
   - [ ] Создать структуру `web/` с `frontend/`, `backend/` (или выбрать монолит).
   - [ ] Настроить общие инструменты (pre-commit, lint).

2. 🔐 **Backend Proxy**
   - [ ] Реализовать FastAPI сервис с auth и проксированием.
   - [ ] Тесты + документация (Swagger/OpenAPI).

3. 📱 **Frontend**
   - [ ] Каркас приложения (Next.js, Tailwind).
   - [ ] Компоненты: фильтры, таблица, графики.
   - [ ] Авторизация и хранение токена (cookie/local storage + refresh strategy).

4. 🚀 **Deployment**
   - [ ] Dockerfiles (frontend/backend).
   - [ ] Railway: 2 сервиса или монолит.
   - [ ] CI/CD pipeline.

5. 📚 **Docs & QA**
   - [ ] Обновить `README` (корневой и `web/`).
   - [ ] Инструкция для пользователей.
   - [ ] Набор регрессионных тестов (Playwright scripts).

---

## 6. Риски и смягчение

| Риск | Вероятность | Митигация |
|------|-------------|-----------|
| Лишний доступ токена в браузере | Средняя | Хранить только на backend, выдавать короткоживущий JWT для UI |
| Ограничения Proxy API | Низкая | Добавить кеширование, оптимизировать SELECT запросы |
| Mobile UX сложный | Средняя | Использовать Tailwind + готовые компоненты, провести user testing |
| Railway ограничения по ресурсу | Низкая | Планировать на Starter/Pro тариф, мониторить нагрузку |

---

## 7. Следующие шаги (на завтра)
1. Решить окончательный стек (Next.js + FastAPI или монолит Next.js API).
2. Подготовить skeleton проекта (`web/backend`, `web/frontend`).
3. Настроить базовые Dockerfiles.

---

**Документ служит рабочим планом для ветки `web-proxy-ui`.** Обновляйте по мере прогресса.

