# 🚀 Railway Deployment Guide

Подробная пошаговая инструкция по развёртыванию backend (FastAPI прокси) и frontend (Next.js UI) на Railway. Рассчитано на начинающих. В конце приведён вариант для нового Flask монолита (`webapp/`).

## 📦 Структура проекта

```
web/
├── backend/    # FastAPI-прокси между браузером и Firebird Proxy API
└── frontend/   # Next.js 14 dashboard
```

Backend и frontend деплоятся отдельными сервисами на Railway, каждый использует свой Dockerfile.

---

## 1. Подготовка

### 1.1. Требования
- GitHub-репозиторий: https://github.com/IvanBondarenkoIT/granit-db-analize-sales (ветка `web-proxy-ui` содержит актуальный код).
- Аккаунт на https://railway.com (достаточно бесплатного плана для начала).
- Доступ к токенам Firebird Proxy API (выдаются администратором).

### 1.2. Сбор данных
Заранее приготовьте и запишите в безопасном месте:
- `PROXY_API_URL` — например `http://85.114.224.45:8000`.
- `PROXY_PRIMARY_TOKEN` — основной токен (64 символа).
- `PROXY_FALLBACK_TOKEN` — дополнительный токен (опционально).
- `SECRET_KEY` — случайная строка 32+ символов для FastAPI (можно сгенерировать командой `python -c "import secrets; print(secrets.token_hex(32))"`).

> ⚠️ Никогда не храните токены в открытом виде в репозитории.

---

## 2. Деплой backend (FastAPI)

### 2.1. Локальная проверка (по желанию)
```bash
cd web/backend
cp env.example env               # создаём файл env
# отредактируйте env и вставьте значения PROXY_* и SECRET_KEY

python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
uvicorn app.main:app --host 0.0.0.0 --port 8001
```
Затем откройте `http://127.0.0.1:8001/health` → должен прийти JSON с `status: "ok"`.

### 2.2. Создание сервиса на Railway
1. Зайдите на https://railway.com → **New Project** → **Deploy from GitHub** → выберите репозиторий `granit-db-analize-sales`.
2. После импорта на главной странице проекта нажмите **New Service** → **Deploy Docker Image** → выберите файл `web/backend/Dockerfile`. Railway автоматически соберёт образ из этого Dockerfile.
3. Перейдите во вкладку **Variables** и добавьте переменные окружения:
   - `SECRET_KEY` — скопируйте сгенерированное значение.
   - `PROXY_API_URL`
   - `PROXY_PRIMARY_TOKEN`
   - `PROXY_FALLBACK_TOKEN` (если есть)
   - `PROXY_TIMEOUT` (например `30`)
   - `ALLOWED_ORIGINS` (пока можно указать `http://localhost:3000`; позже обновим на URL фронта).
4. Нажмите **Redeploy** или дождитесь автоматического деплоя.

### 2.3. Получение URL
После успешного деплоя Railway выдаст домен вида `https://<backend>.up.railway.app`. Запишите его — он понадобится фронтенду.

### 2.4. Проверка
- Во вкладке **Deployments** смотрите логи, убедитесь, что нет ошибок (например, `Proxy API error` → неправильные токены).
- Перейдите по URL `/health` в браузере: `https://<backend>.up.railway.app/health` → должен возвращать JSON.

---

## 3. Деплой frontend (Next.js)

### 3.1. Локальная проверка (по желанию)
```bash
cd web/frontend
cp env.example .env.local
# установите NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8001 (или Railway backend URL)

npm install
npm run dev
```
Откройте `http://localhost:3000` и убедитесь, что дашборд показывает данные (при условии работающего backend).

### 3.2. Добавление сервиса на Railway
1. В том же Railway-проекте нажмите **New Service** → **Deploy Docker Image** → выберите `web/frontend/Dockerfile`.
2. Во вкладке **Variables** добавьте переменную `NEXT_PUBLIC_API_BASE_URL` со значением URL backend (например, `https://<backend>.up.railway.app`).
3. Запустите деплой. По окончании Railway выдаст домен вида `https://<frontend>.up.railway.app`.

### 3.3. Настройка CORS
Вернитесь к backend-сервису и обновите `ALLOWED_ORIGINS`, добавив URL фронта. Пример: `https://<frontend>.up.railway.app`. Если нужно указать несколько доменов — разделяйте запятыми (`https://...,https://...`). После изменения переменной перезапустите backend.

---

## 4. Финальная проверка

1. Откройте фронтенд URL. Карточки и таблица должны отображать данные.
2. При ошибках (например, красный текст «Ошибка получения данных») смотрите логи backend-сервиса.
3. Дополнительно проверьте `GET /stores` и `GET /sales` напрямую через браузер или Postman (не забудьте указать query-параметры `store_ids`, `start_date`, `end_date`).

---

## 5. Советы и дальнейшая работа

- **Автодеплой:** в настройках Railway можно включить автообновление при пуше в определённую ветку.
- **Авторизация:** текущая версия открыта для всех. Рекомендуется позже добавить auth (например, JWT, Basic Auth или Google OAuth на фронте).
- **Мониторинг:** рассмотрите Sentry или Railway alerts.
- **Кэширование:** можно добавить кэш для `/stores` и предопределённых отчётов на backend (Redis или in-memory).

---

## 6. Ссылки
- Railway docs: https://docs.railway.app/
- FastAPI: https://fastapi.tiangolo.com/
- Next.js deployment: https://nextjs.org/docs/app/building-your-application/deploying

---

# 🧩 Приложение Flask (webapp/)

> Этот раздел относится к новой ветке `flask-monolith`, где backend и frontend объединены в одном приложении Flask. Используется папка `webapp/`.

## 📦 Структура

```
webapp/
├── app.py            # Flask-приложение
├── config.py         # Загрузка настроек
├── proxy_client.py   # HTTP-клиент к Firebird Proxy API
├── services/         # Логика агрегации
├── templates/        # Jinja2-шаблоны (Bootstrap)
├── static/           # CSS/JS
├── requirements.txt
├── env.example       # пример переменных окружения
└── Dockerfile
```

## 1. Подготовка

- Токены Proxy API те же (`PROXY_API_URL`, `PROXY_PRIMARY_TOKEN`, `PROXY_FALLBACK_TOKEN`).
- Сгенерируйте `SECRET_KEY` (`python -c "import secrets; print(secrets.token_hex(32))"`).
- Заполните `webapp/env.example` → переименуйте в `env` для локального запуска, либо используйте переменные окружения напрямую.

## 2. Локальный запуск

```bash
cd webapp
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt

cp env.example env   # заполните токены
python app.py
# http://127.0.0.1:8000
```

## 3. Деплой на Railway

Сервис один (Flask + шаблоны):

1. Railway → **New Service** → **Deploy Docker Image** → указать `webapp/Dockerfile`.
2. Во вкладке **Variables** добавить:
   - `SECRET_KEY`
   - `PROXY_API_URL`
   - `PROXY_PRIMARY_TOKEN`
   - `PROXY_FALLBACK_TOKEN` (опционально)
   - `PROXY_TIMEOUT` (например `30`)
3. После деплоя Railway выдаст URL (например `https://flask-app.up.railway.app`).

> CORS и `ALLOWED_ORIGINS` в этом варианте не нужны, так как HTML отдаёт сам Flask.

## 4. Проверка

- Зайдите на выданный домен — должны увидеть дашборд.
- Таблица и фильтры работают за счёт серверной логики.
- При ошибках смотрите логи: вкладка **Deployments** → **Logs**.

## 5. Что далее

- Можно добавить базовую авторизацию (Flask-Login).
- Подключить Celery/APScheduler для периодического кеширования.
- Расширить шаблоны (графики с Chart.js, фильтры, экспорт).

