# 🔧 Настройка алиаса Firebird для БД с кириллицей в пути

## ❗ Проблема

Python/fdb не может правильно передать кириллицу в пути к БД через сеть:
- Путь: `G:\Гранит\GRANITDB\GEORGIA.GDB`
- Ошибка: Кириллица "Гранит" превращается в кракозябры

## ✅ Решение: Создать алиас БД

Алиас - это короткое имя для полного пути к БД, которое настраивается на сервере.

---

## 📋 Инструкция (НА СЕРВЕРЕ)

### ШАГ 1: Найти файл aliases.conf

**Типичные пути:**
```
C:\Program Files\Firebird\Firebird_2_5\aliases.conf
C:\Program Files (x86)\Firebird\Firebird_2_5\aliases.conf
D:\Firebird\aliases.conf
```

**Как найти:**
```powershell
# PowerShell
Get-ChildItem -Path "C:\Program Files" -Recurse -Filter "aliases.conf" -ErrorAction SilentlyContinue
```

---

### ШАГ 2: Открыть aliases.conf

1. Найдите файл `aliases.conf`
2. Откройте **от имени администратора** в Блокноте:
   ```
   Правой кнопкой → Открыть с помощью → Блокнот (от имени администратора)
   ```

---

### ШАГ 3: Добавить алиас

В конец файла добавьте строку:

```ini
# Алиас для БД Гранит
GRANITDB = G:\Гранит\GRANITDB\GEORGIA.GDB
```

**Формат:**
```
АЛИАС = ПОЛНЫЙ_ПУТЬ_К_БД
```

**Важно:**
- Алиас должен быть БЕЗ кириллицы (только латиница)
- Путь может содержать кириллицу
- Без кавычек
- Одна строка

---

### ШАГ 4: Сохранить и перезапустить Firebird

1. **Сохраните** файл `aliases.conf`

2. **Перезапустите службу Firebird:**

```powershell
# PowerShell от администратора
Restart-Service -Name "FirebirdServerDefaultInstance"

# Проверить статус
Get-Service -Name "FirebirdServerDefaultInstance"
```

---

### ШАГ 5: Проверка (ОПЦИОНАЛЬНО, на сервере)

Проверьте подключение к алиасу локально:

```cmd
cd "C:\Program Files\Firebird\Firebird_2_5\bin"
isql -user SYSDBA -password masterkey GRANITDB
```

Если подключение успешно:
```
SQL> SHOW TABLES;
SQL> QUIT;
```

---

## 🧪 Тестирование с вашего ПК

После настройки алиаса на сервере, с вашего компьютера:

```python
import fdb

# Подключение через АЛИАС (вместо полного пути)
conn = fdb.connect(
    dsn="85.114.224.45/3055:GRANITDB",  # Используем алиас!
    user="SYSDBA",
    password="masterkey",
    charset="UTF8"
)

cursor = conn.cursor()
cursor.execute("SELECT COUNT(*) FROM STORGRP")
print(f"Магазинов: {cursor.fetchone()[0]}")
conn.close()
```

---

## 📝 Пример файла aliases.conf

```ini
# Firebird Database Aliases
#
# Format: alias = database path
#

# Примеры:
# employee = C:\Program Files\Firebird\examples\empbuild\employee.fdb
# test = D:\Databases\test.fdb

# Алиас для БД Гранит
GRANITDB = G:\Гранит\GRANITDB\GEORGIA.GDB
```

---

## ⚠️ Важные замечания

1. **Алиас должен быть БЕЗ кириллицы** - только латинские буквы
2. **Путь в aliases.conf может содержать кириллицу** - это нормально
3. **Перезапуск Firebird обязателен** после изменения aliases.conf
4. **Права доступа** - Firebird должен иметь права на чтение файла БД

---

## 🔍 Проверка правильности пути

Если алиас не работает, проверьте:

1. **Файл БД существует:**
   ```powershell
   Test-Path "G:\Гранит\GRANITDB\GEORGIA.GDB"
   ```
   Должно быть: `True`

2. **Firebird имеет права доступа:**
   ```powershell
   # Проверить владельца и права
   Get-Acl "G:\Гранит\GRANITDB\GEORGIA.GDB" | Format-List
   ```

3. **Служба Firebird запущена:**
   ```powershell
   Get-Service -Name "FirebirdServerDefaultInstance"
   ```

---

## 🎯 Альтернативное решение

Если не хотите настраивать алиас, можно:

### Вариант 1: Переместить БД в путь без кириллицы

```powershell
# Создать папку без кириллицы
New-Item -Path "G:\Firebird\Data" -ItemType Directory -Force

# Скопировать БД
Copy-Item "G:\Гранит\GRANITDB\GEORGIA.GDB" "G:\Firebird\Data\GEORGIA.GDB"
```

Тогда путь будет: `G:\Firebird\Data\GEORGIA.GDB`

### Вариант 2: Создать символическую ссылку

```powershell
# Создать симлинк без кириллицы
New-Item -ItemType SymbolicLink -Path "G:\Granit" -Target "G:\Гранит"
```

Тогда путь будет: `G:\Granit\GRANITDB\GEORGIA.GDB`

---

## ✅ Рекомендация

**ЛУЧШИЙ ВАРИАНТ: Использовать алиас**

Преимущества:
- ✅ Не нужно перемещать файлы
- ✅ Короткое имя для подключения
- ✅ Легко менять путь в будущем
- ✅ Стандартный способ в Firebird

---

## 📞 После настройки

Когда настроите алиас на сервере, сообщите мне:
- ✅ Алиас создан (какое имя использовали)
- ✅ Firebird перезапущен
- ✅ Тест подключения прошел

Я обновлю код для использования алиаса!

