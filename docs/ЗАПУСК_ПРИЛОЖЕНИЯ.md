# 🚀 ИНСТРУКЦИИ ПО ЗАПУСКУ GUI ПРИЛОЖЕНИЯ

## 📁 Расположение проекта
```
d:\Cursor Projects\Granit DB analize sales\
```

## 🎯 Способы запуска

### 1. PowerShell скрипт (РЕКОМЕНДУЕТСЯ)
```powershell
# Двойной клик на файл или в PowerShell:
.\launch_gui.ps1
```

### 2. Батч-файл (Windows)
```cmd
# Двойной клик на файл или в командной строке:
launch_gui.bat
```

### 3. Ручной запуск
```cmd
# Откройте командную строку и выполните:
cd "d:\Cursor Projects\Granit DB analize sales"
python run_gui_with_logs.py
```

## ⚠️ Решение проблем

### Проблема: "Файл не найден"
**Причина:** Команда выполняется из неправильной директории

**Решение:**
1. Убедитесь, что вы находитесь в директории проекта
2. Используйте PowerShell скрипт `launch_gui.ps1` - он автоматически переходит в правильную директорию

### Проблема: "Модуль не найден"
**Причина:** Не активировано виртуальное окружение

**Решение:**
1. PowerShell скрипт автоматически активирует виртуальное окружение
2. Или вручную: `venv\Scripts\activate.bat`

### Проблема: "Ошибка подключения к БД"
**Причина:** Неправильный путь к базе данных

**Решение:**
1. Проверьте файл `config.env`
2. Убедитесь, что база данных существует по пути `D:\Granit DB\GEORGIA.GDB`

## 📋 Требования

- Python 3.13+
- Виртуальное окружение (создается автоматически)
- База данных Firebird: `D:\Granit DB\GEORGIA.GDB`
- Права доступа к файлам проекта

## 🔧 Настройка

1. **База данных:** Убедитесь, что файл `D:\Granit DB\GEORGIA.GDB` существует
2. **Конфигурация:** Проверьте файл `config.env`:
   ```
   DB_PATH=D:\Granit DB\GEORGIA.GDB
   DB_USER=SYSDBA
   DB_PASSWORD=masterkey
   DB_CHARSET=UTF8
   ```

## 📊 Функции приложения

- ✅ Подключение к базе данных Firebird
- ✅ Выбор магазинов и периода анализа
- ✅ Генерация отчетов с правильными расчетами
- ✅ Отображение валюты в лари
- ✅ Экспорт в Excel
- ✅ Безопасное отключение от БД
- ✅ Подробное логирование

## 🆘 Поддержка

При возникновении проблем:
1. Проверьте логи в папке `logs/`
2. Убедитесь, что все файлы проекта на месте
3. Проверьте права доступа к базе данных
4. Используйте PowerShell скрипт для автоматического решения проблем

---
**Создано:** 02.10.2025  
**Версия:** 1.0  
**Статус:** Готово к использованию ✅
