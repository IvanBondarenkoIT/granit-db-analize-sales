# ==================== FIREBIRD PROXY API ====================

# Базовая конфигурация для подключения через REST API.
# Скопируйте этот файл в proxy_api.env и заполните секреты.

# ------------------------------------------------------------
# API сервер (может отличаться в зависимости от среды)
PROXY_API_URL=http://85.114.224.45:8000

# Токен доступа (Bearer). Замените на выданный вам секрет!
# Никогда не коммитьте реальные токены в репозиторий.
PROXY_API_TOKEN=REPLACE_WITH_YOUR_TOKEN

# Таймауты и повторы
PROXY_API_TIMEOUT=30
PROXY_API_MAX_RETRIES=3

# Логирование (INFO / DEBUG)
PROXY_API_LOG_LEVEL=INFO


