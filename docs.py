#!/usr/bin/env python3
"""
Coffee Sales Analysis Tool - Documentation Generator

Генерация документации для проекта.
"""

import os
import sys
from pathlib import Path
from datetime import datetime

def create_api_docs():
    """Создание API документации."""
    print("📚 Создание API документации...")
    
    docs_dir = Path("docs")
    docs_dir.mkdir(exist_ok=True)
    
    # API документация
    api_doc = docs_dir / "API.md"
    
    with open(api_doc, 'w', encoding='utf-8') as f:
        f.write(f"""# API Documentation

Coffee Sales Analysis Tool - API Reference

Generated: {datetime.now().strftime("%Y-%m-%d %H:%M:%S")}

## DatabaseConnector

### Methods

#### `__init__(db_path=None, user=None, password=None)`
Инициализация подключения к базе данных.

**Parameters:**
- `db_path` (str): Путь к базе данных Firebird
- `user` (str): Имя пользователя
- `password` (str): Пароль

#### `connect()`
Установка подключения к базе данных.

**Returns:** bool - True если подключение успешно

#### `disconnect()`
Закрытие подключения к базе данных.

#### `test_connection()`
Тестирование подключения к базе данных.

**Returns:** bool - True если подключение работает

#### `execute_query(query, params=None)`
Выполнение SQL запроса.

**Parameters:**
- `query` (str): SQL запрос
- `params` (tuple): Параметры запроса

**Returns:** list - Результат запроса

#### `get_coffee_sales_with_packages(start_date, end_date)`
Получение данных о продажах кофе с упаковками.

**Parameters:**
- `start_date` (str): Начальная дата (YYYY-MM-DD)
- `end_date` (str): Конечная дата (YYYY-MM-DD)

**Returns:** pandas.DataFrame - Данные о продажах

## CoffeeAnalysis

### Methods

#### `__init__(db_connector)`
Инициализация анализатора.

**Parameters:**
- `db_connector` (DatabaseConnector): Подключение к БД

#### `get_sales_statistics()`
Получение статистики продаж.

**Returns:** dict - Статистика продаж

## CoffeeAnalysisGUI

### Methods

#### `__init__()`
Инициализация GUI приложения.

#### `run()`
Запуск GUI приложения.

#### `connect_to_db()`
Подключение к базе данных.

#### `generate_report()`
Генерация отчета.

#### `export_to_excel()`
Экспорт данных в Excel.

## LoggerConfig

### Functions

#### `setup_logger(name='coffee_analysis', level=logging.INFO)`
Настройка системы логирования.

**Parameters:**
- `name` (str): Имя логгера
- `level` (int): Уровень логирования

**Returns:** logging.Logger - Настроенный логгер
""")
    
    print("✅ Создана API документация")

def create_user_guide():
    """Создание руководства пользователя."""
    print("\n📖 Создание руководства пользователя...")
    
    user_guide = docs_dir / "USER_GUIDE.md"
    
    with open(user_guide, 'w', encoding='utf-8') as f:
        f.write(f"""# Руководство пользователя

Coffee Sales Analysis Tool - Руководство пользователя

Generated: {datetime.now().strftime("%Y-%m-%d %H:%M:%S")}

## Содержание

1. [Установка](#установка)
2. [Настройка](#настройка)
3. [Запуск приложения](#запуск-приложения)
4. [Работа с GUI](#работа-с-gui)
5. [Генерация отчетов](#генерация-отчетов)
6. [Экспорт данных](#экспорт-данных)
7. [Устранение неполадок](#устранение-неполадок)

## Установка

### Системные требования

- Python 3.8 или выше
- Windows 10/11 или Linux
- 4 GB RAM (рекомендуется 8 GB)
- 1 GB свободного места на диске

### Установка зависимостей

1. Клонируйте репозиторий
2. Перейдите в директорию проекта
3. Запустите установку:
   ```bash
   python install.py
   ```

## Настройка

### Конфигурация базы данных

Отредактируйте файл `config.env`:

```env
DB_PATH=D:\\Granit DB\\GEORGIA.GDB
DB_USER=SYSDBA
DB_PASSWORD=masterkey
DB_CHARSET=UTF8
```

### Настройка приложения

Дополнительные параметры в `config.env`:

```env
APP_DEBUG=False
LOG_LEVEL=INFO
GUI_THEME=default
GUI_WINDOW_SIZE=1200x800
```

## Запуск приложения

### GUI приложение

```bash
python main.py
# или
python run_gui.py
```

### Консольное приложение

```bash
python run_console.py
```

### С логами

```bash
python run_with_logs.py
```

## Работа с GUI

### Главное окно

1. **Подключение к БД** - нажмите "Подключиться к БД"
2. **Выбор магазинов** - отметьте нужные магазины
3. **Выбор периода** - укажите даты начала и конца
4. **Генерация отчета** - нажмите "Сгенерировать отчет"
5. **Экспорт** - нажмите "Экспорт в Excel"

### Настройки отчета

- **Группировка по дням** - отчет по дням
- **Группировка по неделям** - отчет по неделям
- **Группировка по месяцам** - отчет по месяцам

### Стиль отображения

- **Детальный** - многострочные ячейки
- **Компактный** - однострочные ячейки с эмодзи

## Генерация отчетов

### Типы отчетов

1. **Отчет по продажам** - общая статистика
2. **Отчет по магазинам** - данные по магазинам
3. **Отчет по периодам** - данные по времени

### Параметры отчета

- **Период** - даты начала и конца
- **Магазины** - выбранные магазины
- **Группировка** - дни/недели/месяцы
- **Формат** - детальный/компактный

## Экспорт данных

### Форматы экспорта

- **Excel** - .xlsx файл
- **CSV** - .csv файл (планируется)

### Настройки экспорта

- **Кодировка** - UTF-8
- **Разделитель** - запятая
- **Лист** - название листа Excel

## Устранение неполадок

### Проблемы с подключением к БД

1. Проверьте путь к базе данных
2. Проверьте учетные данные
3. Убедитесь, что база данных доступна

### Проблемы с GUI

1. Перезапустите приложение
2. Проверьте логи в папке `logs/`
3. Обновите зависимости

### Проблемы с отчетами

1. Проверьте выбранный период
2. Убедитесь, что есть данные за период
3. Проверьте подключение к БД

## Поддержка

Для получения поддержки:

1. Проверьте логи приложения
2. Обратитесь к документации
3. Создайте issue в репозитории

## Лицензия

Проект создан для внутреннего использования компании "Дом Кофе".
""")
    
    print("✅ Создано руководство пользователя")

def create_developer_guide():
    """Создание руководства разработчика."""
    print("\n👨‍💻 Создание руководства разработчика...")
    
    dev_guide = docs_dir / "DEVELOPER_GUIDE.md"
    
    with open(dev_guide, 'w', encoding='utf-8') as f:
        f.write(f"""# Руководство разработчика

Coffee Sales Analysis Tool - Руководство разработчика

Generated: {datetime.now().strftime("%Y-%m-%d %H:%M:%S")}

## Содержание

1. [Архитектура проекта](#архитектура-проекта)
2. [Структура кода](#структура-кода)
3. [Разработка](#разработка)
4. [Тестирование](#тестирование)
5. [Сборка](#сборка)
6. [Развертывание](#развертывание)

## Архитектура проекта

### Компоненты

1. **DatabaseConnector** - подключение к БД
2. **CoffeeAnalysis** - анализ данных
3. **CoffeeAnalysisGUI** - GUI интерфейс
4. **LoggerConfig** - система логирования

### Поток данных

```
GUI -> CoffeeAnalysis -> DatabaseConnector -> Firebird DB
  |         |                    |
  |         |                    v
  |         |              Результаты
  |         v
  |    Анализ данных
  v
Отчеты
```

## Структура кода

### Основные модули

- `src/database_connector.py` - работа с БД
- `src/coffee_analysis.py` - анализ данных
- `src/gui_app.py` - GUI приложение
- `src/logger_config.py` - логирование

### Вспомогательные модули

- `main.py` - точка входа
- `run_gui.py` - запуск GUI
- `run_console.py` - консольное приложение
- `test_app.py` - тестирование

## Разработка

### Настройка окружения

1. Установите Python 3.8+
2. Создайте виртуальное окружение
3. Установите зависимости
4. Настройте IDE

### Стиль кода

- PEP 8 для Python
- Документация в формате Google
- Типизация с помощью type hints
- Логирование через logger

### Git workflow

1. Создайте feature ветку
2. Внесите изменения
3. Напишите тесты
4. Создайте pull request

## Тестирование

### Запуск тестов

```bash
python test_app.py
```

### Типы тестов

1. **Unit тесты** - тестирование функций
2. **Integration тесты** - тестирование интеграций
3. **GUI тесты** - тестирование интерфейса

### Покрытие кода

```bash
python -m pytest --cov=src
```

## Сборка

### Создание релиза

```bash
python release.py
```

### Сборка исполняемого файла

```bash
pyinstaller main.py --onefile
```

## Развертывание

### Локальное развертывание

1. Скопируйте файлы проекта
2. Запустите `install.py`
3. Настройте `config.env`
4. Запустите приложение

### Продакшен развертывание

1. Создайте релиз
2. Разверните на сервере
3. Настройте мониторинг
4. Настройте резервное копирование

## Мониторинг

### Логи

- Логи приложения в `logs/`
- Ротация логов по размеру
- Уровни логирования

### Метрики

- Использование памяти
- Время выполнения запросов
- Количество ошибок

## Безопасность

### Защита данных

- Шифрование паролей
- Безопасное хранение конфигурации
- Валидация входных данных

### Аудит

- Логирование всех операций
- Отслеживание изменений
- Резервное копирование

## Производительность

### Оптимизация

- Кэширование запросов
- Индексы базы данных
- Асинхронные операции

### Профилирование

```bash
python -m cProfile main.py
```

## Лицензия

Проект создан для внутреннего использования компании "Дом Кофе".
""")
    
    print("✅ Создано руководство разработчика")

def create_changelog():
    """Создание changelog."""
    print("\n📝 Создание changelog...")
    
    changelog = docs_dir / "CHANGELOG.md"
    
    with open(changelog, 'w', encoding='utf-8') as f:
        f.write(f"""# Changelog

Coffee Sales Analysis Tool - История изменений

Generated: {datetime.now().strftime("%Y-%m-%d %H:%M:%S")}

## [1.0.0] - 2025-10-02

### Added
- Первоначальная версия приложения
- GUI интерфейс на tkinter
- Подключение к базе данных Firebird
- Анализ продаж кофе
- Генерация отчетов
- Экспорт в Excel
- Система логирования
- Консольное приложение
- Тестирование
- Документация

### Features
- Расчет сумм продаж
- Расчет количества чашек
- Расчет килограммов кофе
- Группировка по дням/неделям/месяцам
- Выбор магазинов
- Настройка периодов
- Экспорт данных
- Логирование операций

### Technical
- Python 3.8+
- tkinter для GUI
- pandas для анализа данных
- fdb для работы с Firebird
- openpyxl для Excel
- psutil для мониторинга

## [0.9.0] - 2025-10-01

### Added
- Базовая структура проекта
- Подключение к базе данных
- Основные SQL запросы
- Простой GUI интерфейс

### Fixed
- Ошибки подключения к БД
- Проблемы с кодировкой
- Ошибки в SQL запросах

## [0.8.0] - 2025-09-30

### Added
- Первые тесты
- Базовая документация
- Конфигурация проекта

### Changed
- Структура проекта
- Настройки Git
- Зависимости

## [0.7.0] - 2025-09-29

### Added
- Инициализация проекта
- Настройка окружения
- Первые скрипты

## Unreleased

### Planned
- Улучшение GUI
- Дополнительные отчеты
- Графики и диаграммы
- Настройки через GUI
- Уведомления
- Автоматическое обновление

### Known Issues
- Некоторые расчеты могут быть неточными
- GUI может зависать при больших объемах данных
- Экспорт может быть медленным

## Support

Для получения поддержки обращайтесь к документации или создавайте issues в репозитории.
""")
    
    print("✅ Создан changelog")

def main():
    """Главная функция генерации документации."""
    print("📚 Генерация документации Coffee Sales Analysis Tool...")
    
    try:
        create_api_docs()
        create_user_guide()
        create_developer_guide()
        create_changelog()
        
        print("\n🎉 Документация создана успешно!")
        print("\n📁 Файлы документации:")
        print("- docs/API.md - API документация")
        print("- docs/USER_GUIDE.md - Руководство пользователя")
        print("- docs/DEVELOPER_GUIDE.md - Руководство разработчика")
        print("- docs/CHANGELOG.md - История изменений")
        
        return 0
        
    except Exception as e:
        print(f"❌ Ошибка при создании документации: {e}")
        return 1

if __name__ == "__main__":
    sys.exit(main())

