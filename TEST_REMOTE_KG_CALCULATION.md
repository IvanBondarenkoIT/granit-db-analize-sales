# 🧪 Тестирование расчета килограммов для удаленной БД

## 📋 Что изменено:

### Обновлен метод `_get_remote_sales_data()` в `src/gui_app.py`:

**Было:**
```python
# Упрощенный запрос без килограммов
query = """
SELECT 
    stgp.name as STORE_NAME,
    D.DAT_ as ORDER_DATE,
    COUNT(*) AS ALLCUP,
    0 AS PACKAGES_KG,  # ❌ Всегда 0
    SUM(D.SUMMA) AS TOTAL_CASH
FROM STORZAKAZDT D 
...
"""
```

**Стало:**
```python
# Два запроса: чашки+суммы и килограммы отдельно
cups_query = """..."""      # Чашки и суммы
packages_query = """..."""  # Килограммы пачек кофе

# Объединение результатов через merge
df = df_cups.merge(df_packages, on=['STORE_NAME', 'ORDER_DATE'], how='left')
```

---

## 🧪 Как протестировать:

### В GUI (уже запущен):

1. **Переключитесь на "Удаленная БД (только чтение)"**

2. **Подключитесь:**
   - Сервер: 85.114.224.45
   - Порт: 3055
   - Алиас: DK_GEORGIA

3. **Выберите параметры:**
   - Магазины: выберите все или несколько
   - Период: например, 2025-09-14 - 2025-10-14

4. **Нажмите "Сгенерировать отчет"**

5. **Проверьте колонку "Килограммы":**
   - ✅ Должны быть числа (не нули)
   - ✅ Значения должны быть реалистичными
   - ✅ Сравните с локальной БД для проверки

---

## 📊 Что проверяем:

### Колонки в отчете:

| Колонка | Локальная БД | Удаленная БД (было) | Удаленная БД (стало) |
|---------|--------------|---------------------|----------------------|
| **Магазин** | ✅ Название | ✅ Название | ✅ Название |
| **Дата** | ✅ Дата | ✅ Дата | ✅ Дата |
| **Чашки** | ✅ Точное (ALLCUP) | ⚠️ Приблиз. (COUNT) | ⚠️ Приблиз. (COUNT) |
| **Килограммы** | ✅ Точный расчет | ❌ Всегда 0 | ✅ Расчет из STORZDTGDS |
| **Сумма** | ✅ Точная (CASH) | ✅ Точная (SUMMA) | ✅ Точная (SUMMA) |

---

## 🔍 Логика расчета килограммов:

### Запрос для килограммов:
```sql
SELECT 
    stgp.name as STORE_NAME,
    D.DAT_ as ORDER_DATE,
    SUM(GD.SOURCE) as PACKAGES_KG
FROM STORZAKAZDT D 
JOIN STORZDTGDS GD ON D.ID = GD.SZID 
JOIN Goods G ON GD.GodsID = G.ID 
JOIN STORGRP stgp ON D.STORGRPID = stgp.id 
WHERE D.STORGRPID IN (...)
AND D.CSDTKTHBID IN ('1', '2', '3') 
AND D.DAT_ >= '...' AND D.DAT_ <= '...'
AND (
    -- Пачки кофе с весом в названии
    (G.NAME LIKE '%250 g%' OR G.NAME LIKE '%250г%' OR
     G.NAME LIKE '%500 g%' OR G.NAME LIKE '%500г%' OR
     G.NAME LIKE '%1 kg%' OR G.NAME LIKE '%1кг%' OR
     G.NAME LIKE '%200 g%' OR G.NAME LIKE '%200г%' OR
     G.NAME LIKE '%125 g%' OR G.NAME LIKE '%125г%')
    AND (G.NAME LIKE '%Coffee%' OR G.NAME LIKE '%кофе%' OR 
         G.NAME LIKE '%Кофе%' OR G.NAME LIKE '%Blaser%')
)
GROUP BY stgp.name, D.DAT_
```

**Считает:**
- Пачки кофе 250г, 500г, 1кг, 200г, 125г
- Только товары с "Coffee", "Blaser" в названии
- Суммирует количество (SOURCE) из таблицы товаров

---

## ✅ Ожидаемые результаты:

### Пример отчета:

| Магазин | Дата | Чашки | Килограммы | Сумма |
|---------|------|-------|------------|-------|
| DK Paliashvili | 2025-09-14 | 45 | **2.5** | 1250.00 |
| BatumiMall | 2025-09-14 | 38 | **1.75** | 980.50 |
| CityMall | 2025-09-15 | 52 | **3.0** | 1450.00 |

**Килограммы должны быть > 0 для дней когда продавались пачки!**

---

## 🐛 Если килограммы всё равно 0:

### Возможные причины:

1. **GUI не перезагрузился:**
   - Закройте GUI полностью
   - Запустите заново: `python run_gui.py`

2. **Нет данных о пачках:**
   - Проверьте период (может быть пачки не продавались)
   - Попробуйте больший период

3. **Таблица STORZDTGDS пустая:**
   - Проверьте наличие таблицы в удаленной БД

---

## 📝 Проверка в логах:

Откройте: `logs/coffee_analysis_2025-10-14.log`

**Ищите строки:**
```
INFO - _get_remote_sales_data - Выполнение запроса чашек и сумм...
INFO - _get_remote_sales_data - Выполнение запроса килограммов...
INFO - _get_remote_sales_data - Получено X записей из удаленной БД
```

**Если есть ошибки:**
- Будет `ERROR` с описанием проблемы
- Полный traceback для отладки

---

## 🎯 Сравнение с локальной БД:

### Для точной проверки:

1. **Сгенерируйте отчет из локальной БД:**
   - Период: 2025-09-14 - 2025-10-14
   - Все магазины
   - Запомните значения килограммов

2. **Сгенерируйте отчет из удаленной БД:**
   - Тот же период
   - Те же магазины
   - Сравните килограммы

**Значения должны быть близкими!** (могут немного отличаться если локальная БД старее)

---

## ✅ Критерии успеха:

- ✅ Отчет генерируется без ошибок
- ✅ Килограммы > 0 (хотя бы для некоторых дней)
- ✅ Значения реалистичные (0.25 - 10 кг в день)
- ✅ Чашки и суммы корректные
- ✅ Нет ошибок в логах

---

## 🚀 Готово к тестированию!

**GUI запущен и ждет ваших тестов!**

Протестируйте и сообщите результат! 😊

